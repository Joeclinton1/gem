# Build Instruction Images

Place final assembly renders or photos in this folder using the filenames referenced by
`assets/data/build-steps.json`.

The companion assembly tool repo created at `C:\github_personal\gem-assembly-studio`
is designed to export these images with hidden parts, slide directions, and fastening
arrows.
